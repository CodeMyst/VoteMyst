<nav>
    <a href="/">Home</a>

    @if (ViewBag.HasBrowseableEvents)
    {
        <a href="/events">Browse Events</a>
    }

    @if (ViewBag.HasCurrentEvent)
    {
        <a href=@(ViewBag.IsVotingOpen ? "/vote" : $"/events/{ViewBag.CurrentEventId}")>Current Event</a>
        if (ViewBag.IsSubmissionOpen) 
        {
            <a href="/submit">Submit Entry</a>
        }
    }

    @if (ViewBag.HasOutlineEntries)
    {
        <div class="admin">
            @if (ViewBag.CanCreateEvents)
            {
                <a href="/events/new">Create Event</a>
            }
            @if (ViewBag.CanModifyUser)
            {
                <a href="/users">Find User</a>
            }
        </div>
    }

</nav>