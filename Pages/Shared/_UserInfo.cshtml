@using VoteMyst.Database

@model UserAccount

<a class="user-info" href="@Model.GetUrl()">
    <partial name="_Avatar" for="@Model" />
    <p>@Model.Username</p>
    @* <p class="user-badge @Model.AccountBadge.ToString().ToLower()">
        @Model.AccountBadge
    </p> *@
</a>