@using VoteMyst.Database;

@inject UserProfileBuilder ProfileBuilder
@{
    var user = ProfileBuilder.FromPrincipal(User);
}

<ul>
    <li><a href="/">home</a></li>
    <li><a href="/events">events</a></li>

    @if (user.Permissions.HasFlag(GlobalPermissions.ManageUsers))
    {
        <li><a href="/users/search">find user</a></li>
    }
</ul>